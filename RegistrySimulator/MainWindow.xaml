<Window x:Class="RegistrySimulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:RegistrySimulator"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="360">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="125" />
            <RowDefinition Height="150" />
            <RowDefinition Height="150" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <!-- Content for the first row -->
        <TextBlock x:Name="axTextBlock" Grid.Row="0" Margin="0,10,0,95" Text="AX: " />
        <TextBlock x:Name="bxTextBlock" Grid.Row="0" Margin="0,37,0,70" Text="BX: " />
        <TextBlock x:Name="cxTextBlock" Grid.Row="0" Margin="0,63,0,45" Text="CX: " />
        <TextBlock x:Name="dxTextBlock" Grid.Row="0" Margin="0,89,0,20" Text="DX: " />

        <!-- Content for the second row -->
        <TextBlock Grid.Row="1" Margin="10,10,10,110" Text="Wprowadzanie danych" TextAlignment="Center" />
        <TextBox Grid.Row="1" Margin="50,62,170,62" Width="80" Height="25"/>
        <ComboBox x:Name="insertComboBox"  Grid.Row="1" Margin="170,62,50,62" Width="80" Height="25" SelectedIndex="0"/>
        <Button Grid.Row="1" Content="Wprowadź" Margin="40,88,40,24" Width="200" Height="25" Click="InsertClick"/>
        <Image x:Name="insrtArrowImage" Source="right-arrow.png" Grid.Row="1" Margin="0,62,0,62" Width="20" Height="20"/>

        <!-- Content for the third row -->
        <TextBlock Grid.Row="2" Margin="10,10,10,110" Text="Operacja MOV" TextAlignment="Center" />
        <ComboBox x:Name="movLComboBox"  Grid.Row="2" Margin="50,62,170,62" Width="80" Height="25" SelectedIndex="0"/>
        <ComboBox x:Name="movRComboBox" Grid.Row="2" Margin="170,62,50,62" Width="80" Height="25" SelectedIndex="0"/>
        <Button Grid.Row="2" Content="MOV" Margin="40,88,40,24" Width="200" Height="25"/>
        <Image x:Name="movArrowImage"  Source="right-arrow.png" Grid.Row="2" Margin="0,62,0,62" Width="20" Height="20"/>

    </Grid>
</Window>
